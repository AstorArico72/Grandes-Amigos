@model Grandes_Amigos.Models.ViewModels.EventoConMinisterioViewModel

@{
    var numero = ViewData["EncuentroNro"]?.ToString() ?? "#";
}

<div class="evento-card bg-info bg-opacity-75 text-white rounded shadow-sm p-3 mb-4">
    <div class="row align-items-center g-3">
        <div class="col-auto">
            <div class="bg-white text-primary text-center rounded-3 px-3 py-2">
                <div class="fw-bold fs-5">@numero&#186;</div>
                <small class="text-uppercase fw-bold">Encuentro</small>
            </div>
        </div>
        <div class="col">
            <div class="fw-bold mb-1">
                <i class="bi bi-clock me-1"></i>
                @Model.Evento.Fecha.ToString("dd 'de' MMMM", new System.Globalization.CultureInfo("es-ES")).ToUpper()
            </div>
            <div class="mb-1">
                <strong>Departamento:</strong> @Model.NombreMinisterio
            </div>
            <div>
                <i class="bi bi-geo-alt-fill me-1"></i> @Model.Evento.Título
            </div>
        </div>
        <div class="col-auto">
            <button class="btn btn-success btn-inscribirse"
                    data-evento-id="@Model.Evento.ID"
                    data-bs-toggle="modal"
                    data-bs-target="#inscripcionModal">
                Inscribirse
            </button>
        </div>
    </div>
</div>
