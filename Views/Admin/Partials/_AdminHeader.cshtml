<header class="navbar-admin border-bottom px-3">
    <div class="container-fluid d-flex justify-content-between align-items-center py-2">
        <div class="d-flex align-items-center gap-2">
            <i class="bi bi-speedometer2"></i>
            <h1 class="h6 mb-0 text-light">@ViewData["Title"]</h1>
        </div>

        <div class="d-flex align-items-center gap-3">
            <span class="small text-secondary-200">Sesión: <strong id="kpiSesion">—</strong></span>
            <a href="#" id="btnLogoutJwt" class="btn btn-outline-danger btn-sm">
                <i class="bi bi-box-arrow-right"></i> Salir
            </a>
        </div>
    </div>
</header>
